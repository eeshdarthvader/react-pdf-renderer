/**
 * A React component to view a PDF document
 *

 */

enum SpecialZoomLevel {
    ActualSize = 'ActualSize',
    PageFit = 'PageFit',
    PageWidth = 'PageWidth',
}

export default SpecialZoomLevel;
